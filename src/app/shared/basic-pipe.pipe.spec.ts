import { BasicPipePipe } from './basic-pipe.pipe';

describe('BasicPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BasicPipePipe();
    expect(pipe).toBeTruthy();
  });
});
